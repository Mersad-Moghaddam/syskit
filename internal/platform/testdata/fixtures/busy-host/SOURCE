SOURCE provenance for fixture: busy-host

status:       synthetic fixture (hand-authored, not captured from a live host)
represents:   a busy Linux host (elevated load averages, high CPU jiffy counts)
kernel:       synthetic (placeholder — modelled on Linux 6.x /proc format)
distro:       synthetic (placeholder)
arch:         x86_64 (placeholder)
container:    none (placeholder)
capture-date: synthetic fixture — 2026-07-08 (authored)

files:
  proc/stat     — /proc/stat, 2 logical CPUs, high jiffy counts
  proc/loadavg  — /proc/loadavg, load averages ~3/2/1.6

Byte format mirrors what the Linux kernel emits (whitespace and field order
preserved). Values are illustrative, not measured. Replace with a real capture
via scripts/capture-fixtures.sh once that helper exists.
